//tonnes is {year: tonnes}
const emissionsGraph = () => {
  let data = 
  let width = 400;
  let height = 400;

  let svg = d3.select("#line-graph")
    .append("svg")
    .attr("width", width)
    .attr("height", height)
    .append("g");
};